# Archived local artifacts

These files are retained for traceability only. Current scripts do not read
them, and they must not be used as source inputs or downstream mapping outputs.

- `superseded_intermediate/` — earlier Zingbrain/Contentful intermediates and
  an obsolete fuzzy-review export.

The active workflow and file roles are documented in the repository root
[`README.md`](../README.md).
